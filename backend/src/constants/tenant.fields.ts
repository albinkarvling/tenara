export const TENANT_FIELDS = {
    DEFAULT:
        "id, propertyId, firstName, lastName, age, gender, isMarried, phoneNumber, createdAt, street, zipCode, city, municipality, county, countyCode, createdAt",
} as const;
